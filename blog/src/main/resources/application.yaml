server:
  port: 9090
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/blog?useUnicode=true&useSSL=false&characterEncoding=utf8&serverTimezone=UTC
    username: root
    password: rootroot
mybatis-plus:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.mirror.pojo

